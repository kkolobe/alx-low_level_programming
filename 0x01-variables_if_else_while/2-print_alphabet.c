#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints the alphabets in lowercase
 * Return: Always (success)
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);
}
